# Security hardening for npm
# Prevent install-time code execution (supply chain attack mitigation)
ignore-scripts=true

# Prevent git operations during install
allow-git=none

# Use strict SSL
strict-ssl=true

# Audit level
audit-level=high
